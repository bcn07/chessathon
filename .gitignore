# environments and caches
.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.DS_Store

# generated locally
submission.zip
*.pgn
# platform games with per-move clocks are reference data (1.6 MB), kept like the logs
!reference/games/*.pgn
results/

# reference engines are local builds; only the notes are tracked
tools/engines/*
!tools/engines/README.md
tools/src/

# multi-GB training sets and generated candidates
training/data/
work/

# spare submission archives (submission.zip is the canonical one and is ignored above)
submission-*.zip
